<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
</head>
<body>
    
</body>
</html>

<h1>{{ title }}</h1>
<p>Bem-Vindo a nosso site</p>
<div class="container">
    <ul>
    {# Laço For. Foreach não funciona aqui no TWIG #}
        {% for user in users %}
            <li>Nome: {{ user.name }} - {{ user.cep }}</li>
        {% endfor %}
    </ul>
</div>